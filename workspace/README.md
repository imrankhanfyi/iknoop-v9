# workspace/ — our working layer (not upstream)

Local-only continuity notes for this fork, carried on the `fork/no-network` branch so they
ride every upstream rebase and get backed up to the `personal` remote. **Source/notes only —
no biometric data ever lives here.**

- `state.md` — the running "where we are / what's open" log for the v9 era. Start here when
  resuming a session.

Deep historical detail (the v1.61→v1.68 investigation trail: RTC fix, sleep staging, workout
detection, the original reconnect work) lives in the archived repo's own workspace:
`~/Projects/NOOP-archive/repo-v1.61/workspace/state.md`.
